package com.example.smp.notification.configurations.properties;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class BaseUri {
    // No downstream HTTP services for this microservice.
    // Add base URI fields here when downstream integrations are introduced.
}
